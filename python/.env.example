# Copy this file to .env (or export these values) before running toSQL.py.
# The values below are placeholders only — replace them with your own.
#
# This database must match the Laravel backend's DB_DATABASE so that the
# API can read the ingested readings from the same `inputemission` table.
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=
DB_NAME=energy_dashboard