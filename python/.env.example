# Copy this file to .env (or export these values) before running toSQL.py.
# The values below are placeholders only — replace them with your own.
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=
DB_NAME=emissiondatabase
